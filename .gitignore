# === BIG DATA & TEMP FOLDERS ===
data/original/
data/subset/
data/signed/
liboqs/
tools/
venv/

# === GENERATED RESULTS ===
# Keep these files visible in repository:
# - results.csv, results.json (data)
# - *.png (visualizations)
# - results.tex (LaTeX table)
# - RESULTS.md (report)
# Only ignore if they're in subdirectories
# results/
# output/

# === DOCKER & WINDOWS JUNK ===
# Keep docker-compose.yml and *.bat for reproducibility
fast-subset.sh

# === PYTHON ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.ipynb_checkpoints

# === GIT SYSTEM FILES ===
.gitattributes

# === OS GARBAGE ===
.DS_Store
Thumbs.db
Desktop.ini